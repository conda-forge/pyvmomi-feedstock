{% set name = "pyvmomi" %}
{% set version = "8.0.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6756f06ce0bb88924e5a7e9be95e0856037fdaf08a5b32c0889b9eb060d7c265

build:
  number: 0
  noarch: python
  script: '{{ PYTHON }} -m pip install . -vv '

requirements:
  host:
    - pip
    - python
  run:
    - python
    - requests >=2.3.0
    - six >=1.7.3

test:
  imports:
    - pyVim
    - pyVmomi

about:
  home: https://github.com/vmware/pyvmomi
  license: Apache Software
  license_family: APACHE
  license_file: LICENSE
  summary: VMware vSphere Python SDK

extra:
  recipe-maintainers:
    - pg-hbk
    - sy-hbk
